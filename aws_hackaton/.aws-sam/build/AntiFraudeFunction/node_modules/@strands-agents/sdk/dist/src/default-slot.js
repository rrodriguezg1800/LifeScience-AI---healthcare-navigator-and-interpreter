import { DefaultNotConfiguredError } from './errors.js';
export function createDefaultSlot(errorMessage) {
    // A unique sentinel for "unset" so any value of T -- including undefined or
    // null -- can be stored and read back without colliding with the empty state.
    const EMPTY = Symbol('empty');
    let value = EMPTY;
    return {
        set(v) {
            value = v;
        },
        get() {
            if (value === EMPTY)
                throw new DefaultNotConfiguredError(errorMessage);
            return value;
        },
    };
}
//# sourceMappingURL=default-slot.js.map